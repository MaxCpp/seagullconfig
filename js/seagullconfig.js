var ajaxurl = '/assets/modules/seagullconfig/ajax.php';

$(document).ready(function() {
    // $('.datetimepicker').datetimepicker({dateFormat: 'dd.mm.yy'});
    // $('.ckeditor').ckeditor({width:800, height: 200});
    // $('.b-timepicker').timepicker({timeFormat: 'h:m'});
});
